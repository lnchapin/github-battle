const React = require('react')
const PropTypes = requie('prop-types')



class Loading extends React.Component {
  render() {
    return(
      <p style={}>
        {this.state.text}
      </p>
    )
  }
}

module.exports = Loading;
